# Five-hour study replay

This frozen version preserves the five-hour study independently of the separate 24-hour work in `scripts/`. It includes the 46 seed types, 12 learned types, and the final audit corrections. Run `uv run python research/2026-09-05/amount_outliers_eth_5h/replay/tx_types.py <command>` from the repository root. Defaults point to this study; implied pricing is disabled. The shared modules are copied for reproducibility. The RPC helper resolves the repository root from this location.
